# Redirection spécifique pour les profils vers la fonction Netlify
/api/social/profile/*  /.netlify/functions/profile-proxy/:splat  200!

# Redirection de l'API vers le backend avec CORS headers
/api/*  https://biogy-api.onrender.com/api/:splat  200!
  Access-Control-Allow-Origin: *
  Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS, PATCH
  Access-Control-Allow-Headers: Content-Type, Authorization, X-Requested-With, Accept, Origin
  Access-Control-Allow-Credentials: true
  Access-Control-Max-Age: 86400

# Redirection SPA pour React Router
/*    /index.html   200